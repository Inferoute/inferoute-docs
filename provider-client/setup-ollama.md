# Setup: Ollama

This page moved. Use the **[setup wizard](setup.md)** — it installs Ollama if needed, pulls an approved model, and writes config.

For Docker, if the client runs in a container and Ollama on the host, see [Installation](../getting-started/installation.md#docker).
